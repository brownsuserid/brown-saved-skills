# Create a worktree
I prefer to work in Git Worktrees on my local system.
Use the `wt` command (installed to ~/bin via setup-ai-sdlc.sh) to spin up new worktrees and VS Code.

## Instructions:
1. Create a new worktree with
```bash
wt FEATURENAME
```

