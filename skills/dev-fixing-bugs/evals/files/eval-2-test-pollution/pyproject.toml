[project]
name = "config-manager"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.mypy]
strict = true

[tool.ruff]
line-length = 100

[tool.bandit]
exclude_dirs = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
